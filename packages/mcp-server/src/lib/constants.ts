/**
 * Shared constants for the Sekkei MCP server.
 */

/** Default workspace directory name. Previously "sekkei-docs", now "workspace-docs". */
export const DEFAULT_WORKSPACE_DIR = "workspace-docs";

/** Legacy workspace directory name — checked as fallback for backward compatibility. */
export const LEGACY_WORKSPACE_DIR = "sekkei-docs";
