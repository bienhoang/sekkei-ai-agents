# Phase 2: Adapter SKILL.md sync

**File:** `sekkei/packages/mcp-server/adapters/claude-code/SKILL.md`
**Fixes:** B1, B3, B5, B6 (mirror Phase 1 changes)

## Changes

Mirror all Phase 1 changes to adapter SKILL.md (inlined version):

1. **ut-spec section** — update prerequisite to MCP-based, add test-plan upstream, fix output path to dynamic
2. **it-spec section** — same as ut-spec
3. **st-spec section** — update prerequisite to MCP-based, add test-plan + functions-list as optional upstream
4. **uat-spec section** — update prerequisite to MCP-based, add test-plan + nfr as optional upstream
5. **Document chain diagram** — sync with skills/content/SKILL.md (test specs nested under test-plan)

## Checklist

- [ ] ut-spec: MCP prereq + test-plan upstream + dynamic output path
- [ ] it-spec: MCP prereq + test-plan upstream + dynamic output path
- [ ] st-spec: MCP prereq + test-plan upstream
- [ ] uat-spec: MCP prereq + test-plan upstream
- [ ] Sync diagram
